<h1><%= @user.username %></h1>

    <dl class="dl-horizontal">
      <% @user.photos.each do |photo| %>
      <dt>Caption</dt>
      <dd><%= photo.caption %></dd>

      <dt>Image</dt>
      <dd><image src="<%= photo.image %>"></image></dd>
      <% end %>

    </dl>